module.exports = {
  // The dependency block was removed to fix a warning with recent versions of React Native.
  // Autolinking should be able to find the podspec and Android package automatically.
};
