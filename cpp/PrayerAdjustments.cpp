#include "PrayerAdjustments.h"

namespace adhan {

    PrayerAdjustments::PrayerAdjustments(int fajr, int sunrise, int dhuhr, int asr, int maghrib, int isha)
    : fajr(fajr), sunrise(sunrise), dhuhr(dhuhr), asr(asr), maghrib(maghrib), isha(isha) {}

}